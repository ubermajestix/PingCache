<h2>Locations for Device <%= @device.mac %></h2>

<%if @device_locations %>
<table cellspacing=0 cellpadding=5>
  <%for track in @device_locations %>
    <tr>
      <td><%= track.location_id %></td> <td><%= track.created_at %></td>
    </tr>
  <%end%>
</table>
<% else %>
Device hasn't been anywhere
<% end %>
seen at: <%= @device.locations.map(&:ip).join("</br>") %>